<script setup>
import { ref, onMounted } from 'vue';

// Define a reactive variable for showing the loader
const showLoader = ref(true);

// Hide the loader after 3 seconds
onMounted(() => {
    
});
</script>

<template>
    
</template>